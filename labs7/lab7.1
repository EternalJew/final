#include <cstdlib>
#include <iostream>
#include <math.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */
using namespace std;
int main(int argc, char** argv) {
	double a;
	int i,k,n;
	double*array;
	cout<<"Input size of arroy:";
	cin>>n;
	array=new double[n];
	cout<<"Input elements of array:"<<endl;
	for(i=0;i<n;i++){
		cin>>array[i];
	}
	for(k=1;k<n;k++)
	for(i=0;i<n-k;i++)
	if(array[i]>array[i+1])
	{
		a=array[i];
	array[i]=array[i+1];
	array[i+1]=a;
}
cout<<"Results of sortirovka masiva:";
for(i=0;i<n;i++)cout<<array[i]<<";     ";
cin>>i;
	return 0;